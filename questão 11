programa {
	funcao inicio() {
		inteiro a, b

		escreva("Digite o valor de A: ")
		leia(a)
		
		escreva("Digite o valor de B: ")
		leia(b)

		se ((b != 0 e a % b == 0) ou (a != 0 e b % a == 0))
		{
			escreva("São múltiplos")
		}
		senao
		{
			escreva("Não são múltiplos")
		}
	}
}
